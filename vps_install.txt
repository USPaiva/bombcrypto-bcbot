vps do Google
recomendado usar o processador e3 2v, 8gb e o ubuntu 20.04

sudo apt-get update && sudo apt-get upgrade -y
sudo apt-get install python3-pip
sudo apt-get install wget
sudo apt-get install unzip
wget https://github.com/carecabrilhante/bombcrypto-bcbot/archive/refs/heads/main.zip
unzip main.zip
cd bombcrypto-bot-main
pip3 install tesseract
pip3 install -r requeriments.txt --force
sudo apt-get install ubuntu-desktop (caso não tenha interface gráfica)
sudo apt-get update && sudo apt-get upgrade -y
sudo apt-get update && sudo apt-get install -y xrdp && sudo service xrdp restart && sudo -s
passwd
sudo apt-get update && sudo apt-get upgrade -y

caso de erro no bot por causa do telegram, use:
pip3 install --upgrade --force-reinstall python_telegram_bot
